extends layout

block content
    h1 Users
    .w3-container
    for user in users
        ul
          li USERNAME
            = user.username
          li NAME
            = user.name
          li COURSE
            = user.course
          li email
            = user.email

